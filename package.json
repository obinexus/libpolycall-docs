{
  "name": "libpolycall-docs-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Gulp asset pipeline for the LibPolyCall Jekyll documentation UI.",
  "scripts": {
    "build:assets": "gulp build",
    "watch:assets": "gulp watch",
    "clean:assets": "gulp clean"
  },
  "devDependencies": {
    "del": "^7.1.0",
    "gulp": "^5.0.0",
    "gulp-clean-css": "^4.3.0",
    "gulp-sass": "^5.1.0",
    "sass": "^1.83.0"
  }
}
